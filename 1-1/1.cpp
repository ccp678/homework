#include <iostream>
#include<cstring>
#include<cmath>
using namespace std;


int main()
{/*
    int i = 0;
    
//这个本质上是一个进制转化问题，就是一个26进制转化为10进制，然后计算差

    string s1,s2;
    int pos1=0,pos2 = 0;
    cout<<"输入两个字符串"<<endl;
    cin>>s1;
    cin>>s2;
    for(i=0;i < s1.size()-1;++i)
    {
        pos1+=s1[i] - 'A'+1;
        pos1*=26;
    }
    pos1+=s1[i] - 'A';
    for(i = 0;i < s2.size()-1;++i)
    {
        pos2 += s2[i] - 'A'+1;
        pos2*=26;
    }
    pos2+=s2[i]-'A';
    int out = fabs(pos1 - pos2) - 1;
    cout<<out<<endl;
    cout<<fabs(pos1-pos2)-1;          //减一试一为了算除去首尾的序列的个数
*/
cout<<int(-3.4);
}